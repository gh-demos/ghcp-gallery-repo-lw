import Link from 'next/link';
import { Plus } from 'lucide-react';
import { GalleryTable, Hero, SectionContainer, SectionTitle } from '@/components/ui';
import { mockGalleries } from '@/lib/mock-gallery-data';

export default function AdminGalleriesPage() {
  return (
    <div className="page-gradient">
      <Hero
        title="Manage Galleries"
        description="Review existing galleries and create new collections for clients and portfolio updates."
      />

      <SectionContainer>
        <SectionTitle title="All Galleries" viewAllLink="/admin">
          <Link href="/admin/galleries/new" className="btn-primary inline-flex items-center gap-2">
            <Plus className="h-4 w-4" />
            Create New Gallery
          </Link>
        </SectionTitle>

        <GalleryTable galleries={mockGalleries} />
      </SectionContainer>
    </div>
  );
}
